'UTILITY AND CONFIG'
class "GameConfig<static>" as GameConfig{
  + int MAX_TIME
  + int FIREBALL_MULTIPLIER
  + int FIREBALL_INCREMENT
  + int FIREBALL_THRESHOLD
  + int BACKBOARD_FREQUENCY
  + Dictionary<BonusRarity, RarityMetadata> RARITY_METADATA
}
enum BonusRarity{
  Common
  Uncommon
  Rare
  Epic
  Legendary
}
struct RarityMetadata{
  + Color color
  + int threshold
  + int pointBonus
}
class "Ballistics<static>" as Ballistics{
  + bool, Vector3 SolveArcTargetAngle(Vector3 s, Vector3 t, float a, float g)
  + Vector3 ReflectPointAcrossPlane(Vector3 p, Vector3 plane, Vector3 normal)
}
class "RandomPoint<static>" as RandomPoint{
  + Vector3 RandomPointInBox(GameObject g1, GameObject g2)
  + Vector3 RandomPointInBox(Vector3 g1, Vector3 g2)
  
}
enum "ShotType<flags>" as ShotType{
  None
  Perfect
  Backboard
  HoopTouch
}
enum ScoreType{
  Scored
  Missed
}


class "GameState<pure class>" as GameState{
  + int Score
  + int RemainingTime
  + int FireballValue
  + bool FireballStatus
  
  + event OnScoreChange
  + event OnRemainingTimeChange
  + event OnTimeEnd
  + event OnFireballChange
  + event OnFireballEnable
  + event OnFireballDisable
  
  + void AddTime(int time)
  + void RemoveTime(int time)
  + void AddFireball(int value)
  + void ResetFireball()
  + void AddScore(int score)
  + void ResetScore()
  - void Fireball(bool enable)
}
class "GameplayController<singleton>" as GameplayController{
  + GameplayController Instance
  
  + event OnGameStart
  + event OnGameEnd

  + void StartGame()
  + void EndGame()
  - void TimeStep()
  
}

'PLAYER CLASSES'
class InputHandler{
  - IM_InputMapping playerControls
  - Camera mainCamera

  + event OnStartTouch
  + event OnEndTouch
  + event OnJump
  + event OnMove
  + event OnLook
  + event OnThrowBall
  + event OnThrowUpdate
  + event OnThrowEnd
  + event OnThrowCancel

  - void PrimaryContactStart()
  - void PrimaryContactEnd()
  - void MouseToggle()
  - void JumpPerformed()
  - voic ThrowBallPerformed()
  + void SwipeUpdate(float distance)
  + void SwipeEnd(Vector2 direction, float distance)
  + void SwipeCancel()
  + Vector3 ScreenToWorld(Camera camera, Vector3 position)
  + Vector3 Primary3DPosition()
  + Vector2 Primary2DPosition()
}
class SwipeDetection{
  - Vector2 startPosition
  - float startTime
  - Vector2 endPosition
  - float endTime
  - float minimumDistance
  - float maximumTime
  - bool trailEnabled
  - TrailRenderer trail
  
  - void SwipeStart(Vector2 position, float time)
  - void SwipeEnd(Vector2 position, float time)
  - void DetectSwipe()
}
class PlayerController{
  - GameObject head
  - CharacterController characterController
  - float movementSpeed
  - float rotationSpeed
  - float jumpForce
  - float gravity
  
  + void Move()
  + void Look()
  + void Jump()
  + void ThrowBallEnd(Vector2 direction, float power)
  //+ void ThrowBall()
}

'UI ELEMENTS'
class GameplayUI{
  - TMP TMP_Time
  - TMP TMP_FireballMeter
  - TMP TMP_FireballStatus
  - TMP TMP_FireballMultiplier
  - TMP TMP_Score
  - TMP TMP_PowerMeter
  - Slider Slider_PowerMeter
  - Slider Slider_FireballMeter
  - InputHandler inputHandler
  
  - void SetTimeLeft(int time)
  - void SetScore(int score)
  - void SetFireballValue(int value)
  - void SetFireballEnable()
  - void SetFireballDisable()
  - void SetPowerValue(float value)
  - void ResetPowerValue()
}
note left of GameplayUI::inputHandler
to retrieve slide data
end note
class RewardUI{
  - TMP finalScore
  
  + void SetFinalScore(int score)
}
class FloaterTextController{
  - TMP TMP_BallPoints
  - float autoDisable
  - bool playOnStart
  - GameObject _rotateTowards
  - bool autoRotateTowards
  
  + void DisplayPoints(int points)
}
note left of FloaterTextController::TMP_BallPoints
- animated no auto loop
end note
note left of FloaterTextController::autoRotateTowards
- rotate text to object on update
end note
class ScoreFXController{
  - ParticleSystem particleSystem
  
  + void PlayFX();
}


'BASKET RELATED CLASSES'
class BasketballBoard{
  - TMP TMP_bonus
  - int rollFrequency
  + int PointBonus
  
  + BonusRarity SelectRarity()
  + void UpdateText(BonusRarity)
  + void SelectApplyRarity()
}
class BasketballFactory{
  - GameObject basketballPrefab
  - Vector3 spawnOffset
  
  + GameObject CreateBasketball()
  + GameObject CreateBasketball(Vector3 position, Quaternion rotation)
  + GameObject DelayDestroy(GameObject o, float delay)
}
class BasketballTracker{
  - CinemachineVirtualCamera ballCamera
  - Basketball trackedBall
  
  + void TrackBasketball(Basketball basketball)
  - void ResetTrackingCamera(Basketball despawned_ball)
}
class BallisticLauncher{
  + Transform Target
  + Transform ReflectAgainst
  + float ArrivalAngle
  + AnimationCurve AimAssistCurve
  + float AimAssistRange
  
  + Vector3 AimAssist(Vector3 raw, Vector3 optimal, float range, AnimationCurve assist_curve)
  + bool LaunchGameObject(GameObject o, LaunchMode m, float forward_offset, float power)
  - bool LaunchDirect(GameObject o, float power)
  - bool LaunchReflect(GameObject o, float forward_offset, float power)
  
  + static bool LaunchDirect(...)
  + static bool LaunchReflect(...)
}
note right of BallisticLauncher::GraceCurve
Alters the power
makes it easier to score
end note
enum LaunchMode {
  Direct
  Reflect
}
class Basketball{
  - ParticleSystem fireTrail
  + int BallPoints
  + float BallDiameter
  - bool isValid
  - bool isLegal
  - bool autoDelete
  - int lifetime
  
  + event OnBasketballDestroy
  + event OnBasketballScore
  
  - void OnCollisionEnter() 
  - void OnTriggerEnter()
}
note left of Basketball::OnCollisionEnter()
- Detect hoop collision
- Detect board collision
end note
note left of Basketball::OnTriggerEnter()
- Validation trigger
- Score trigger
end note










PlayerController o-- BasketballFactory
PlayerController o-- BallisticLauncher
PlayerController o-- InputHandler
PlayerController o-- BasketballTracker

BasketballTracker o-- Basketball


PlayerController ..> GameplayController

SwipeDetection --> InputHandler

GameplayController *-- GameState
GameState ..> GameConfig
GameConfig -> BonusRarity
GameConfig *-- RarityMetadata

GameplayUI ..> GameplayController
GameplayUI ..> GameConfig
RewardUI ..> GameplayController

BasketballFactory ..> Basketball

Basketball ..> BasketballHoop
Basketball ..> BasketballBoard
Basketball ..> GameplayController
Basketball -> ShotType
Basketball -> ScoreType
Basketball ...> FloaterTextController
Basketball ...> ScoreFXController


BallisticLauncher --> Ballistics
BallisticLauncher -l-> LaunchMode 




















